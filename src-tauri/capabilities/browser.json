{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "browser",
  "description": "Capability for the in-app browser window (NotebookLM etc.)",
  "windows": ["nlm-browser"],
  "permissions": [
    "core:default"
  ]
}
